{
  "buildCommand": "git lfs install && git lfs pull && npm run build",
  "installCommand": "git lfs install && git lfs pull && npm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "public": "public",
  "ignoreCommand": "git lfs ls-files | cat"
} 